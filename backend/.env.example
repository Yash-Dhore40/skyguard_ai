# Port provided automatically by cloud platforms like Render / Heroku / Railway
PORT=8000

# Public URL of the backend (Render provides RENDER_EXTERNAL_URL automatically)
# If deploying elsewhere or using a custom domain, set SELF_PING_URL to prevent idle sleep
SELF_PING_URL=https://your-backend-service.onrender.com
